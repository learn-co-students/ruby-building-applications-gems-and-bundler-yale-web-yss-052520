# source "https://rubygems.org"
# gem 'awesome_print', '~> 1.8'
# the addtional '~> 1.8' indicates what verison this file will run on

# gem 'rack', git: 'https://github.com/rack/rack'
# ^put this line (leading to github repository) if the respoitory is fro the gem only
    # used when the gem isn't on the default source (first line)

    # if the repository is private you can access via SSH key with the following line
    # gem 'nokogiri', :git => 'login@example.com:some-user-account/some-private-gem.git'

source "https://rubygems.org"
# gem "rspec"

gem "hashie"
gem "sinatra", "2.0.2"
gem "octokit", "~> 2.0"
gem "awesome_print", :git => "git@github.com:awesome-print/awesome_print.git"
     
group :development do
    gem "pry"
end

group :test do
    gem "rspec"
end
